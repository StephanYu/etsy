
<div class="jumbotron">
  <div class="container">
    <%= t('jumbotron_title_html') %>
    <%= t('jumbotron_subtitle_html') %>
  </div>
</div>

<div class="container" id="main-container">
  <div class="center">
    <div class="row">
      <% unless params[:category].blank? %>
        <h3><%= @category.name.capitalize %></h3>
      <% end %>

      <% @listings.each do |listing| %>
        <div class="col-md-3">
          <div class="thumbnail">
            <%= link_to image_tag(listing.image.url), listing %>
            <div class="caption">
              <h3><%= listing.name.capitalize %></h3>
              <p id="listing-price-thumbnail"><%= number_to_currency(listing.price) %></p>
              <p id="listing-author-thumbnail"><%= t('posted_by', user: "#{current_user.first_name.capitalize} #{current_user.last_name.capitalize[0]}.") if current_user %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <br>
  <%= will_paginate @listings, renderer: BootstrapPagination::Rails %>
  <br>

  <% if user_signed_in? %>
    <%= link_to t('add_listing'), new_listing_path, class: 'btn btn-link', data: { no_turbolink: true } %>
  <% end %>
</div>
